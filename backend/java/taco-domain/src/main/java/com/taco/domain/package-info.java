/**
 * TACO Domain 层
 *
 * 领域层职责：
 * - 定义领域实体（Entity）
 * - 定义值对象（Value Object）
 * - 定义仓储接口（Repository Interface）
 * - 定义领域服务（Domain Service）
 * - 定义领域事件（Domain Event）
 *
 * 设计原则：
 * - 不依赖任何外部框架
 * - 纯 Java 实现
 * - 包含核心业务逻辑
 */
package com.taco.domain;